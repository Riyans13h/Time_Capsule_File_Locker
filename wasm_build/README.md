# Time Capsule Core WebAssembly Build

This directory contains the build configuration for compiling the C++ core functionality to WebAssembly.

## Prerequisites

1. Install Emscripten SDK (emsdk)
2. Activate the latest Emscripten environment

## Building

Run the build script:

```bash
chmod +x build.sh
./build.sh